<div class="span8">
	<h1><%= Date.current %></h1>
			
	<table class="table table-bordered table-hover table-striped">
		<% @tasks.each do |t| %>
			<tr>
				<td><%= link_to "#{t.room}", task_path(t) %></td>
				<td><%= t.begin %> - <%= t.end %></td>
				<td><%= t.gear %></td>
				<td><%= t.preset %></td>
				<td><%= t.setup %></td>
				<td><%= t.strike %></td>
				<td><%= t.notes %></td>
			</tr>
		<% end %>
	</table>
</div>
		
<aside class="span3 newTask well">
	<h3>New Task</h3>
	<%= render 'shared/task_form', locals: { task: @task } %>
</aside>
